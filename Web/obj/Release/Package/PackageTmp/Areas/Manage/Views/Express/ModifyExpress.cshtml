@{
    ViewBag.Title = "修改快递";
    Layout = "~/Areas/Manage/Views/Shared/_Layout.cshtml";
}
@section header{
    <style type="text/css">
        .search { margin: 10px auto 0px auto; }
        .txt { width: 150px; }
    </style>
    <script type="text/javascript">
        $.ready(function () {

            var vld=new $.Validation();
            vld.add("txtName",{ emptyAble: false,emptyText: "不可为空" });

            $("btnAdd").on("click",function () {
                if(vld.validate()) {
                    $.post("/Manage/Express/ModifyExpress/",function (res) {

                        if(res.success) {
                            alert("修改成功！");
                            closeDialog(true);
                        } else
                            alert(res.msg);
                    },{
                        id: @ViewBag.id,
                        name: $("txtName").value
                    });
                }
            });

        });

       
    </script>
}
<table class="search">
    <tr>
        <th>快递名称：</th>
        <td><input type="text" id="txtName" value="@ViewBag.name" class="txt" /></td>
    </tr>
</table>
<div class="buttons"><a id="btnAdd" href="javascript:void(0);" class="btn1"><em>修改快递</em></a></div>
